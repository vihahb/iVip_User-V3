<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">
    <!--tools:openDrawer="start"-->

    <include
        layout="@layout/activity_home_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <android.support.design.widget.NavigationView
        android:id="@+id/home.navigationView"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="@mipmap/bg_nav"
        android:fitsSystemWindows="true"
        android:theme="@style/AppTheme.AppBarOverlay.NavigationView"
        app:itemBackground="@drawable/navigation_item_background"
        app:itemIconTint="@android:color/white"
        app:itemTextColor="@android:color/white"
        app:menu="@menu/nav_home"
        app:theme="@style/AppTheme.AppBarOverlay.NavigationView" />
</android.support.v4.widget.DrawerLayout>